# Towards Transparent Credit Risk Assessment: An Explainable AI Framework for Financially Excluded Borrowers


Authors: Raashi Dhamange, Shristi Dhamange, Chaitalee Mali (AISSMS Institute of Information Technology, Pune, India)

This repository contains the complete, runnable pipeline used to produce every
table and every figure in the paper. Nothing here is hand-drawn: every number
and every image is generated by a script that you can re-run yourself.

---

## 1. What this repository reproduces

| Paper artefact | Script | Output |
|---|---|---|
| Fig. 1 — System architecture | `scripts/10_system_architecture_diagram.py` | `outputs/figures/fig1_system_architecture.png` |
| Table II — Model comparison | `scripts/02_train_models.py` | `outputs/tables/table_II_model_comparison.csv` |
| Fig. 2 — ROC curves | `scripts/03_generate_roc_curves.py` | `outputs/figures/fig2_roc_curves.png` |
| Fig. 3 — Metrics bar chart | `scripts/04_generate_metrics_comparison.py` | `outputs/figures/fig3_metrics_comparison.png` |
| Fig. 4 — Global SHAP summary | `scripts/05_shap_summary.py` | `outputs/figures/fig4_shap_summary.png` |
| Fig. 5 — Local SHAP waterfall | `scripts/06_shap_waterfall.py` | `outputs/figures/fig5_shap_waterfall.png` |
| Fig. 6 — SHAP vs LIME | `scripts/07_shap_vs_lime.py` | `outputs/figures/fig6_shap_vs_lime.png` |
| Table III / Fig. 7 — Fairness audit | `scripts/08_fairness_audit.py` | `outputs/tables/table_III_fairness_metrics.csv`, `outputs/figures/fig7_fairness_approval_rates.png` |
| Table IV / Fig. 8 — Thin-file alt-data | `scripts/09_thin_file_alt_data.py` | `outputs/tables/table_IV_thin_file_alt_data.csv`, `outputs/figures/fig8_thin_file_altdata.png` |
| Fig. 9 — Loan-officer dashboard | `dashboard/dashboard.html` | Open directly in any browser |

Full mapping with paper section numbers: [`docs/FIGURE_MAPPING.md`](docs/FIGURE_MAPPING.md).

---

## 2. Repository structure

```
xai-credit-risk-assessment/
├── README.md                    <- you are here
├── requirements.txt
├── LICENSE
├── .gitignore
├── data/
│   ├── raw/german.csv           <- bundled dataset (1000 rows, 21 columns)
│   └── processed/                <- generated at run time (gitignored)
├── src/                          <- importable pipeline library
│   ├── config.py                 <- paths, seed, column names, assumptions
│   ├── data_loader.py
│   ├── preprocessing.py
│   ├── models.py
│   ├── evaluate.py
│   └── utils/logger.py
├── scripts/                      <- one numbered script per paper artefact
│   ├── run_all.py                <- runs everything end to end
│   ├── 01_download_data.py       <- optional: refresh data/raw/german.csv
│   ├── 02_train_models.py  ... 10_system_architecture_diagram.py
├── dashboard/
│   └── dashboard.html            <- Fig. 9, standalone, no server needed
├── outputs/
│   ├── figures/                  <- all 9 figures, PNG, regenerated each run
│   ├── tables/                   <- all 4 tables, CSV, regenerated each run
│   └── logs/                     <- one log file per script run
├── docs/
│   ├── ASSUMPTIONS.md            <- every place we had to fill a gap, and why
│   ├── REPRODUCIBILITY.md        <- step-by-step: VS Code, Colab, GitHub
│   ├── FIGURE_MAPPING.md         <- script <-> paper section/figure/table
│   └── OPTIONAL_EXTENSIONS.md    <- suggested follow-up experiments
└── tests/
    └── test_pipeline.py          <- smoke tests for the pipeline
```

---

## 3. Quickstart

```bash
git clone https://github.com/dhamangeraashi-bit/xai-credit-risk-assessment.git
cd xai-credit-risk-assessment
pip install -r requirements.txt
python scripts/run_all.py
```

That's it — every table lands in `outputs/tables/`, every figure in
`outputs/figures/`, and `dashboard/dashboard.html` can be opened directly.
For a fully explicit, screenshot-level walkthrough (including Google Colab
and VS Code specifically), see [`docs/REPRODUCIBILITY.md`](docs/REPRODUCIBILITY.md).

---

## 4. Honesty about numbers

Re-running this pipeline reproduces the paper's qualitative conclusions
faithfully and its quantitative results closely (most metrics within 1-2
percentage points; XGBoost recall matches to three decimal places: 0.937).
It does **not** reproduce every number to the last decimal, because the
paper does not publish a random seed or full hyperparameter grid. Every
place we had to make an assumption to fill that gap is listed, with its
justification, in [`docs/ASSUMPTIONS.md`](docs/ASSUMPTIONS.md) — including
the one case (fairness audit direction, Section VII-B) where our honestly-computed
numbers diverge from the paper's more than we'd like, which we report
plainly rather than hide.

---

## 5. Dataset

Statlog (German Credit Data), Hofmann (1994), UCI Machine Learning
Repository, DOI: 10.24432/C5NC77. Bundled at `data/raw/german.csv` (mirrored
from a public GitHub source, see `scripts/01_download_data.py`) so the
pipeline works fully offline.

## 6. License

MIT — see [`LICENSE`](LICENSE). Please cite Hofmann (1994) / UCI if you reuse the dataset.

## 7. Citation

If you use this code, please cite the accompanying paper (see the manuscript
for full citation details) and this repository.
